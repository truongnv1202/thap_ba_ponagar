# Dev: không cần dòng này — mặc định dùng proxy Vite (same-origin /api).
# Gọi thẳng backend (khi không dùng proxy): 
# VITE_API_BASE=http://localhost:4000
# Build rồi `vite preview` cùng proxy: để trống:
# VITE_API_BASE=
